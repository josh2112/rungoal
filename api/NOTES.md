# Alembic

Generate migration script: `alembic revision --autogenerate -m "<description>"`

Upgrade to revision: `alembic upgrade <version>|head`

# Rungoal CLI

Commans from `rungoal.cli` are available through `pyproject.toml` as `rungoal-cli.exe`
