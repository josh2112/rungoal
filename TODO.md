- Heatmap - don't hardcode starting coordinates
  - Compute & store a bounding box with each run
  - Initially set the heatmap view to the most recent run's center

- Calendar view: Simple calendar month view with run days circled. Tapping a day takes you to the runs for that day.
